# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage.html
coverage.txt

# Build output
/out/
/msf-catalog-validator

# Python virtual environment for pre-commit
venv/

# Go workspace file (local dev convenience; not published)
go.work
go.work.sum

# Dependency directories (remove the comment below to include it)
vendor/

.DS_Store
